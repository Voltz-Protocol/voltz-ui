query GetSwaps($positionId: String!) {
  swaps(where: { position: $positionId }) {
    id
    transaction {
      id
      blockNumber
      timestamp
    }
    amm {
      id
    }
    position {
      id
    }
    sender
    txIndex
    sqrtPriceX96
    liquidity
    tick
  }
}
