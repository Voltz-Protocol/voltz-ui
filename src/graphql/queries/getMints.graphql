query GetMints($positionId: String!) {
  mints(where: { position: $positionId } ) {
    id
    transaction {
      id
      blockNumber
      timestamp
    }
    amm {
      id
    }
    position {
      ids
    }
    sender
    amount
  }
}
