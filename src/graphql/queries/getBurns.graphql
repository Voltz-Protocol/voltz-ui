query GetBurns($positionId: String) {
  burns(where: { position: $positionId } ) {
    id
    transaction {
      id
      blockNumber
      timestamp
    }
    amm {
      id
    }
    position {
      id
    }
    sender
    amount
  }
}